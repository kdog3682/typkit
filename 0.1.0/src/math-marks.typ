#import "misc.typ": wrap
#import "ink.typ": *

#let spacer = h(3pt)
#let dot = wrap(sym.circle.filled.tiny, spacer)
#let equals = wrap(sym.eq, spacer)
#let arrow = wrap(sym.arrow, spacer)
#let plus = wrap(sym.plus, spacer)
#let minus = wrap(sym.minus, spacer)

