#let table = table
#let content = content
#let stack = stack
#let align = align
#let box = box
#let block = block
#let scale = scale
#let rect = rect
#let circle = circle
#let line = line
#let polygon = polygon
#let box = box
#let length = length
#let measure = measure
#let table = table
#let text = text
#let range = range
#let place = place
#let underline = underline
#let stack = stack
#let left = left
#let right = right
#let top = top
#let bottom = bottom
#let middle = center + horizon
#let center = center
#let horizon = horizon
#let blue = blue
#let red = red
#let green = green
#let purple = purple
#let black = black
#let gray = gray
#let rotate = rotate
#let columns = columns
#let path = curve
#let curve = curve
#let tiling = tiling


// #table(align: 1, column-gutter: 1, columns: 1, fill: 1, gutter: 1, inset: 1, row-gutter: 1, rows: 1, stroke: 1)
// #box(baseline: 1, clip: 1, fill: 1, height: 1, inset: 1, outset: 1, radius: 1, stroke: 1, width: 1)
// #text(alternates: 1, baseline: 1, bottom-edge: 1, cjk-latin-spacing: 1, costs: 1, dir: 1, discretionary-ligatures: 1, fallback: 1, features: 1, fill: 1, font: 1, fractions: 1, historical-ligatures: 1, hyphenate: 1, kerning: 1, lang: 1, ligatures: 1, number-type: 1, number-width: 1, overhang: 1, region: 1, script: 1, size: 1, slashed-zero: 1, spacing: 1, stretch: 1, stroke: 1, style: 1, stylistic-set: 1, top-edge: 1, tracking: 1, weight: 1)
// #stack(dir: 1, spacing: 1)
// #circle(fill: 1, height: 1, inset: 1, outset: 1, radius: 1, stroke: 1, width: 1, content)
