
#let soft = (
    dash: "densely-dotted",
    thickness: 0.5pt,
)
#let dense = (
    dash: "densely-dotted",
    thickness: 0.5pt,
)
#let gentle = (
    thickness: 0.5pt,
    dash: "loosely-dotted",
)
#let verysoft = (
    thickness: 0.35pt,
    dash: "densely-dotted",
)
#let thin = black + 0.5pt
#let solid = black + 0.5pt
#let normal = black
